# Compatibility and Smoke-Test Matrix / 相容性與人工測試矩陣

> Last reviewed: 2026-09-22, reflecting live `v1.9.4` evidence from a Windows zh-TW session and the unreleased boot-rotation fix on `main`. This document records evidence, not a guarantee. Provider DOM and login flows can change without notice.

## Status legend

- **Verified** — exercised manually on the named platform or validated by a focused automated test.
- **Partially verified** — real-device evidence exists, but at least one named path remains unverified or blocked.
- **CI-only** — compiles/packages in GitHub Actions, but no maintainer end-user launch report is available.
- **Pending** — needs a repeatable manual check before claiming support for that behavior.

## Desktop platforms

| Platform | Packaging evidence | End-user launch | Status |
|---|---|---|---|
| Windows x64 | NSIS and portable builds; local development and packaged builds | Maintainer/user runs available | **Verified** |
| macOS Apple Silicon | DMG builds in CI; embedded app is verified as ad-hoc signed | A `v1.0.1` user opened the app and logged into ChatGPT, Claude, and Gemini; Grok looped on Cloudflare verification | **Partially verified** |
| Linux x86_64 | AppImage builds in CI with WebKitGTK dependencies | No maintainer desktop report yet | **CI-only** |

macOS remains ad-hoc signed, not Developer ID signed or notarized. The Apple Silicon report confirms that the documented first-launch exception works, but does not make the build warning-free. Current source leaves provider permission APIs untouched, permits Cloudflare's required `about:blank` / `about:srcdoc` documents, defers the automation bridge on any detected Cloudflare or hCaptcha security-check page, and never monkey-patches Grok's History API. A native Tauri title observer marks known Grok challenge titles as blocked and authorizes the delayed bridge only after a positive app-title event; an ambiguous document receives no automatic DOM probe or script evaluation. The v1.8.8 candidate instead offers an explicit stale-state reconnect that recreates the Grok child webview with its persistent profile after the guarded watchdog window. Automated tests cover this policy, but no new Apple Silicon end-user launch or provider-login smoke has been recorded for the v1.8.8 candidate. The macOS retest, especially confirmation that Grok leaves Cloudflare verification, remains **Pending**.

## Agent-ready source lane

| Evidence | Windows | macOS / Linux | Status |
|---|---|---|---|
| Manifest/schema and Skill drift tests | 22 focused tests pass locally | The same 22 tests pass in Windows, macOS, and Linux CI jobs | **Verified** as a source contract; GUI launch remains separate |
| Doctor/audit/dry-run JSON | Exercised locally; dry-run preserves runtime state | Node contract paths pass on all three CI operating systems | Windows **Verified**; others **CI-only** |
| App-level READY wait | Three live source-launch smokes reached the same-run, identity-verified control-pane READY marker on 2026-07-12; stale/replacement/missing-state tests also pass | No real-device source-launch report | Windows **Verified**; others **Pending** |
| Launch/stop race safety | Live smokes released the fail-closed launch mutex; audit probes detected generated/target/runtime changes; stop re-verified before kill and before same-run state deletion; foreign/EPERM tests pass | Same code path, not manually exercised | Windows **Verified**; others **Pending** |
| Corrupt state recovery | Default stop refused malformed state and preserved it; explicit `--clear-invalid-state` removed only the state file, then a normal launch/stop completed | Not manually exercised | Windows **Verified**; others **Pending** |

The Agent contract does not claim that CI displayed a window. It also does not install host prerequisites, inventory the full OS, sandbox checked-out code, upload receipts, or roll back host changes. See [`AGENT-READY-SOURCE-RELEASE.md`](./AGENT-READY-SOURCE-RELEASE.md).

The v2.0.0 source contract supports Node.js `^22.13.0 || >=24.0.0`, matching the locked pnpm and lint toolchain. `agent:doctor` rejects unsupported Node versions and stops instead of presenting an invalid source launch as ready. This requirement applies only to source development; packaged desktop users do not need Node.js.

## Provider adapters

| Provider | Bundled adapter | Windows text workflow evidence | Image-only completion |
|---|---:|---|---|
| ChatGPT | v7 + engine compatibility fallback | v4 text workflow **Verified**; v5 mismatch recovery and v6 logged-out precedence have automated coverage; the v7 login-form detector was checked against live `/auth/login`, logged-out root, and logged-in DOM states on Windows. A v1.8.8 Roundtable trace showed a 4,992-character handoff rendered as an optimistic, collapsed `Show more` user turn while the exact draft remained in the composer; the engine treated that bubble alone as send confirmation, then Astra kept working after the abandoned wait emitted `[Error: bridge degraded]`. Focused coverage now requires the new matching turn plus a consumed/changed composer or a post-anchor response, recognizes the current composer submit control, accepts only a sufficiently long collapsed prompt prefix beyond the pre-send baseline, and keeps exact plain-text `Pro thinking` activity alive inside the current turn. That required live retest ran on Windows zh-TW with `v1.9.3` on 2026-09-21 and **failed**: ChatGPT rendered a complete reply on screen, but nothing returned to the app and the run stopped there. `v1.9.3` latched the user-turn anchor onto ChatGPT's optimistic bubble; ChatGPT then replaced that bubble with a collapsed turn, the anchor node detached, and every live reply failed the follows-the-anchor test, so no chunk was emitted, the completion check was never armed, and the turn-completion timeout stayed disabled until the 600-second host step timeout. `v1.9.4` keeps the adopted anchor latched, ignores an anchor node that has left the document, and permits an anchorless read only after generation is observed with this wait's pre-send baseline already snapshotted. The required live retest ran as a Roundtable on Windows zh-TW with published `v1.9.4` on 2026-09-22 and **passed** for capture and completion across collapsed turns. One unchanged bootId `r3tn4wtt` completed all three rounds: round 1 「開場立論」 sent 5,439 characters and reached `RESPONSE_DONE` at 1,228 characters in 157.0 s; round 2 「交叉質疑」 sent 9,848 characters and reached `RESPONSE_DONE` at 1,683 characters in 178.1 s; round 3 「攻防深化」 sent 15,408 characters and reached `RESPONSE_DONE` at 1,988 characters in 253.0 s. Round 2 is exactly the turn class `v1.9.3` lost. Prompts of 9,848 and 15,408 characters are long enough to collapse behind `Show more`, which is what the smoke step requires. This was the owner's own Roundtable run, not the scripted smoke step executed clause by clause. It demonstrates capture and completion across collapsed turns. It does not separately evidence the "do not click Send manually" clause or the "intermediate text reaches the app while `Pro thinking` remains active" clause. | Partial manual coverage; recheck after provider UI changes |
| Claude | v4 | v3 text workflow **Verified**; v4 login-page detection and explicit Google SSO scope have automated coverage and await live retest | Not a compatibility claim |
| Gemini | v2 | Base text workflow **Verified**; bounded Google `/sorry` navigation, blocked status, and passive bridge behavior have automated coverage and await live retest | Not a compatibility claim |
| Grok | v7 + engine compatibility fallback | Base text workflow **Verified**; focused tests recognize all three current textarea composer selectors, preserve exact Unicode/Markdown input across a composer remount, detect and stop the current `chat-stop-button`, and keep login ready during the composer-to-generation-control transition. Host tests cover an explicit guarded reconnect for the unchanged-title bridge wedge without evaluating an ambiguous challenge document. A v1.8.8 Windows first-login trace showed the session persisted but the final `auth.x.ai` device-verification navigation was denied, requiring an app restart. That host remains denied and external per the frozen navigation contract; only the exact default HTTPS origin now triggers a bounded return to the allowed `grok.com` app surface using the same persistent profile. `v1.9.1` adds automated coverage that cancels stale quiet timers when Grok restores its stop control and resumes with unchanged intermediate text (poll and mutation paths, including two resume cycles). Heavy phase boundaries and the no-restart first-login repair still need a live retest | Not a compatibility claim |
| Meta AI (experimental standby) | v2 seed | Logged-out Windows DOM verified for the inert composer, Send control, login detector, and Meta auth redirect origins. Native input injection, exact-four standby enforcement, and selector schema checks have automated coverage. Facebook/Instagram HTTPS login hosts are bundled SSO so a live Log in redirect stays in the isolated Meta profile; a system-browser login cannot return. A Windows zh-TW `v1.9.3` live smoke on 2026-09-21 confirmed authenticated send, receive, and completion: Meta reported `logged_in` and finished two Roundtable speeches (909 characters in 31.8 s and 1,333 characters in 33.4 s, each delivered as ten chunks with a terminal done signal). The same Windows zh-TW Roundtable on published `v1.9.4` on 2026-09-22, the same run as the ChatGPT retest, completed three Meta rounds as the prompt grew and stalled on round 4. Round 1 sent 4,742 characters and reached `RESPONSE_DONE` at 676 characters in 26.0 s; round 2 sent 8,811 characters and reached `RESPONSE_DONE` at 1,016 characters in 33.5 s; round 3 sent 14,165 characters and reached `RESPONSE_DONE` at 1,222 characters in 47.0 s (round 3 spent roughly 35 s with the bridge marked degraded; the outbox backed up to 8 entries and then flushed all at once). Round 4 sent 21,233 characters at 05:01:13.858 on bootId `ts684yo5` and never completed. The last engine status from `ts684yo5` was 05:01:06.016. No engine status arrived for 73.3 s after that, spanning the whole composer-fill window. The webview was then replaced twice: `4vpb89uv` at 05:02:34.452, with a `logged_out` flap, and `d5yqanjo` at 05:02:40.646. No `RESPONSE_DONE` for round 4 was emitted. The new page reported `thinking: true` at 05:03:20.667 because the owner manually pasted into the provider window to try to rescue the step. `src/workflow/` had no `bootId` awareness, so a waiter settled only on `RESPONSE_DONE`, supersede, or timeout. A new engine boots with `waitingForResponse = false`, which gates every capture path, so the replaced page could not finish the old step even in principle. `reportsProviderActivity()` re-armed the 10-minute inactivity timer on `thinking === true`, so the manual paste extended the hang, bounded only by the 60-minute absolute cap. Stop, new-session reset, and profile persistence across a restart still require a live smoke | Not a compatibility claim |

The experimental Meta AI standby work tracks community request [#95](https://github.com/teddashh/multi-ai-chat-desktop/issues/95) and implementation [PR #96](https://github.com/teddashh/multi-ai-chat-desktop/pull/96). The evidence and pending live-smoke requirements below apply to that work.

`v1.9.0` adds automated coverage for Meta replacing each default seat across all workflow presets, snapshot/replay, standby recovery, settings-save races, and unusable composers. It ships Meta as an optional experimental standby; no new authenticated Meta or VM smoke is claimed. See the [release notes](RELEASE_NOTES_v1.9.0.md) for verification results.

`v1.9.1` adds automated coverage for Grok generation resume with unchanged intermediate text. Live-account Grok Heavy ↔ ChatGPT Astra handoff remains pending. See the [release notes](RELEASE_NOTES_v1.9.1.md).

`v1.9.4` recovers ChatGPT response capture when the latched user-turn anchor detaches, and adds focused coverage for a detached-anchor read, an anchorless completion, and a negative case that pre-send assistant output is never emitted through the fallback. See the [release notes](RELEASE_NOTES_v1.9.4.md).

On `main` at `b4f4d1c` ([PR #108](https://github.com/teddashh/multi-ai-chat-desktop/pull/108)), and not in any published release, a boot rotation that replaces a provider's document fails that step with `ProviderPageReloadedError`. The rejection is applied before the status is published, so `thinking` from the new page cannot re-arm the inactivity timer. The prompt is deliberately not re-sent to the new engine: if the original send landed, a re-send would double-send. Debate, Consult, Coding, and Roundtable stop with a localized explanation; Brainstorm gets the existing Retry / Skip / Cancel dialog; Free keeps its per-provider swallow. The same change instruments composer fill as advisory `STATUS_REPORT` fields (`fill`, `fillChars`, `fillMs`), character counts only and never prompt text, so the previously unobservable window between a queued send and the next event is measurable. Meta is the only provider on `inputStrategy: "default"`, which reaches `document.execCommand('insertText', …)` with the whole string; ChatGPT, Claude, and Grok use paste-based `prosemirror-paste`; Gemini uses `quill-angular`. Whether to move Meta off `default` is deliberately deferred until this instrumentation produces a field measurement. This change has automated coverage only. The motivating field record is the published `v1.9.4` Meta round 4 stall in the provider table.

Meta AI VM QC on 2026-09-20 (PR #96, `e3a9f9d`): default standby, exactly four active providers, and standby swap/restore passed. The site's Log in action redirected to Facebook, which then left the embedded allowlist. A 2026-09-21 Windows debug bundle (`v1.9.0`) reproduced that path: `nav-blocked` `www.facebook.com`, Meta stayed `logged_out`, and a system-browser login could not return after restart. Bundled Meta SSO now includes Facebook and Instagram HTTPS hosts so that redirect stays in the Meta profile; the engine is still not injected on those identity pages. Guest or email/mobile login, if offered, also stays in-pane. A 2026-09-21 Windows zh-TW `v1.9.3` bundle then showed Meta `logged_in` and two completed Roundtable speeches, so authenticated send, receive, completion, and multi-seat workflow use are **Verified** on that platform; stop, new-session reset, and profile persistence remain pending. Browser login is not evidence that the embedded provider profile is authenticated.

Automated tests validate adapter structure, schema v1/v2 parser compatibility, typed detector rejection, logged-out precedence, approved strategies, HTTPS URL parsing, and navigation boundaries. They do not log into live provider accounts. Remote adapter updates cannot expand the URL scopes bundled with the installed app.

Claude's current consumer web experience requires an authenticated account. Adapter v4 recognizes common email-login fields and keeps the official Anthropic and Google sign-in routes within the existing bounded SSO policy. The app does not bypass login, age, subscription, challenge, or other provider-side requirements; guided workflows that assign a Claude seat remain blocked until Claude reports a ready composer.

macOS note: the `v1.0.1` report verified ChatGPT, Claude, and Gemini login, but Grok remained on Cloudflare's security-verification page. Current source omits the document-start bridge and permission shim for Grok while retaining the background-liveness settings required by hidden provider panes. A single atomic driver reads the shared Cloudflare/hCaptcha title, body, and marker signals before changing provider state or creating the bridge, then retries unresolved and blocked documents through page-load events and the host watchdog. Known challenge titles include the Traditional Chinese `安全驗證` variant, and an already-running engine refuses fill, send, and stop mutations while a challenge is active. Closing an allowlisted Grok authentication popup can reserve one same-document native reload; owner/epoch gates, close invalidation, rollback, and a bounded navigation-start lease prevent duplicate or permanently wedged recovery without evaluating the blocked document. A Windows 11 test with WebView2 Runtime `150.0.4078.99` completed the live challenge and embedded login using a broader prototype configuration; the final popup-close handoff and non-Windows behavior still require live confirmation. The app does not automate or bypass the challenge.

Gemini may redirect an embedded session to `https://www.google.com/sorry/index?...`. Current source allows only the HTTPS `www.google.com/sorry` path family for Gemini, reports it as blocked instead of logged in, skips the permission shim there, and defers bridge startup until Google returns to Gemini. Sibling paths, lookalike hosts, non-HTTPS URLs, and cross-provider use remain denied. A live challenge completion still requires manual verification.

## Product behavior

| Area | Automated evidence | Manual release check |
|---|---|---|
| Free mode | Four-provider fan-out tests | Send to all selected providers; verify each final response |
| Debate / consultation / coding | Golden graph ordering, prompt threading, four-provider default assignment, unavailable-provider preflight, configurable roles, bounded send retry, and terminal provider-error tests | Complete one default run; verify role labels and the final summary, then confirm a provider error stops before any dependent step |
| Roundtable | Five-round, four-seat history, four-provider default coverage, configurable assignment, repeated-seat preflight, unavailable-provider tests, and terminal provider-error handling | Complete one run; verify prior same-session speeches remain available and a provider error does not enter the next speaker's prompt |
| Brainstorm | Twelve rounds × four rotating seats, four-provider defaults, four distinct lenses, 48-step history threading, five phase prompts, preflight, localization, graph-v4 snapshot compatibility, and explicit Retry/Skip/Cancel recovery tests that keep raw provider errors out of downstream prompts | Allow 45–90 minutes; verify four contributions per round, explicitly skip one limited provider, and confirm the next seat continues toward a consolidated portfolio |
| Structured provider-error handling | Debate, Consult, Coding, and Roundtable fail terminally without unattended recovery; Consult tears down an unfinished parallel sibling immediately. Brainstorm retains explicit Retry/Skip/Cancel recovery. Recovery actions remain owned by `requestId`, FIFO-safe, and protected from stale dialog actions; cancellation settles provider-stop cleanup and the previous run before a replacement starts. | In a standard structured mode, confirm a provider error stops promptly without entering downstream prompts. In Brainstorm, confirm the recovery dialog names the correct provider and exercise Retry, Skip, and Cancel |
| Provider page replaced mid-step | On `main` (`b4f4d1c`, PR #108) only; no published release includes it. Automated tests fail the step with `ProviderPageReloadedError` when a provider document is replaced, apply the rejection before the new page's status is published so `thinking` cannot re-arm the waiter, and do not re-send the prompt. Debate, Consult, Coding, and Roundtable stop with a localized explanation; Brainstorm uses the existing Retry / Skip / Cancel dialog; Free keeps its per-provider swallow. Automated coverage only | Not a current smoke step: no published release includes this behavior. The 2026-09-22 published `v1.9.4` Meta round 4 stall in the provider table is the field record of the hang; this change has no field check |
| ChatGPT handoff identity | Send confirmation requires the matching newly rendered user turn. Tests preserve an exact 15,917-character Unicode/Markdown prompt across a composer remount, bound a normal silent-delivery failure to approximately 10–19 seconds instead of the 600-second response timeout, anchor response capture after the matching user turn, and reject remounted historical output. The v1.8.8 candidate additionally requires the same current assistant turn, unchanged full response text, no strong activity, and a stable completion marker before handing off; a new send is rejected without touching the composer while native generation remains visible. `v1.9.4` adds a detached-anchor read, an anchorless completion, and a negative pre-send case after the 2026-09-21 live attempt stalled with the reply visible only in ChatGPT. | **Pending for v1.8.8:** reproduce the reported Astra multi-phase reasoning and Grok Heavy handoff with real accounts; confirm no partial preamble advances the workflow, no second/third prompt interrupts Astra, and only the terminal answer completes the step. The 2026-09-21 attempt reached ChatGPT but never returned its reply to the app (fixed in `v1.9.4`), and Grok Heavy was not exercised |
| Long provider work | Thinking, pulled chunks, bulk-ready, and done-ready activity refresh a 10-minute inactivity window; ChatGPT completion-marker tests cover more than 10 minutes of active thinking, transient completion controls, full-text rewrites, and missing current-turn structure, then fail closed after true completion inactivity. Successful empty bridge pulls no longer synthesize a degraded error or stop the provider; tests enforce the separate 60-minute workflow hard cap | Run one provider task beyond 10 minutes, then verify a truly stalled task still terminates |
| Diagnostics event retention | Unchanged bridge and connection heartbeats are coalesced independently, so one heartbeat class cannot suppress the other or crowd substantive diagnostics out of the bounded 2,000-entry event buffer | During a slow handoff, inspect a sanitized event log and confirm useful workflow and provider events remain available |
| Session isolation | Conversation persistence and latest-snapshot matching tests | Create two sessions; confirm no messages or export provenance cross over |
| Restored-session continuity | Stable response-identity and bounded same-session replay tests | Reopen a session, ask a follow-up, and confirm old context is available without cross-session leakage |
| Response fidelity | DOM-to-Markdown tests for paragraphs, nested lists, links, fenced code, direct/nested tables, image-only fallback, finish-time revisions/shortening, late render batches, and literal replacement patterns in code | Compare a slowly completed provider answer containing code and a table with the captured transcript; confirm the final provider DOM and transcript match |
| Transcript scrolling | Near-bottom and user-scroll intent tests; scroll-linked provider focus, resize/reflow recalculation, pre-first-message boundary, binary lookup, and maximized-workspace tests | Stream a long answer, scroll upward, resize the window, maximize/restore the transcript, and confirm the provider chip and reading position remain stable |
| Session quota recovery | Quota-only eviction, transient-failure preservation, and persisted-state result tests | Fill local history near quota and confirm only the oldest sessions are removed |
| Snapshot / replay | Schema, redaction, version mismatch, replay, and app-version tests | Save/replay once when local snapshot persistence is enabled |
| Markdown export | Formatting and provenance tests | Confirm UTC time, app version, latest matching workflow/snapshot, and adapter versions |
| Adapter hot update | Rust validation, version gate, cache, and URL-scope tests | Use a higher-version test adapter on an allowed host scope |
| Control-pane security | Capability and CSP configuration tests | Confirm Settings update check and export still work in a packaged build |

## Release smoke checklist

1. Install or launch the platform artifact on a clean profile.
2. Open and authenticate each provider using a non-sensitive test account where possible. Claude requires its official Google or email login flow; do not call Claude ready until the composer appears.
   On Windows, complete Grok's first in-app login and confirm the denied/external `auth.x.ai` device-verification handoff returns the embedded Grok pane to Ready without an app restart; one host-only `nav-blocked` diagnostic is expected. Close an in-app authentication popup after completing login and confirm the blocked pane reloads once and becomes Ready without a manual reload. Separately reproduce an unresolved `狀態過期` state for more than 40 seconds if possible, click the Grok status chip once, and confirm the child webview is recreated with the signed-in profile, reaches Ready, and does not restart a Cloudflare challenge loop.
   On macOS, explicitly confirm that Grok exits the Cloudflare verification page before calling the release verified.
3. Verify prompt insertion, automatic send, thinking state, text completion, and new-session reset.
4. Run Free mode and one serial mode; cancel one in-progress run.
   For ChatGPT Astra/Pro, use a serial handoff long enough for the user message to collapse behind `Show more`; do not click Send manually. Confirm the new user turn is accepted, intermediate text reaches the app while `Pro thinking` remains active, the workflow does not synthesize `bridge degraded` after ten minutes of genuine activity, and the next serial input is withheld until ChatGPT exposes terminal completion. This check ran on `v1.9.3` (Windows zh-TW, 2026-09-21) and failed with the reply visible in ChatGPT but never captured. The rerun on published `v1.9.4` happened on 2026-09-22 (Windows zh-TW Roundtable, one unchanged bootId `r3tn4wtt`) and passed for capture and completion across collapsed turns: round 1 「開場立論」 sent 5,439 characters and reached `RESPONSE_DONE` at 1,228 characters in 157.0 s; round 2 「交叉質疑」 sent 9,848 characters and reached `RESPONSE_DONE` at 1,683 characters in 178.1 s; round 3 「攻防深化」 sent 15,408 characters and reached `RESPONSE_DONE` at 1,988 characters in 253.0 s. Round 2 is exactly the turn class `v1.9.3` lost. Prompts of 9,848 and 15,408 characters are long enough to collapse behind `Show more`, which is what this step requires. This was the owner's own Roundtable run, not the scripted smoke step executed clause by clause. It demonstrates capture and completion across collapsed turns. It does not separately evidence the "do not click Send manually" clause or the "intermediate text reaches the app while `Pro thinking` remains active" clause.
5. For v1.8.8, run a real-account structured handoff from Grok Heavy to ChatGPT Astra and back, allowing both models to finish multi-phase reasoning. Confirm the exact handoff is sent once, an intermediate Grok answer or transient ChatGPT action bar never advances the workflow, and no second/third prompt interrupts Astra. Also confirm a provider error stops a standard structured mode promptly; in Brainstorm, verify Retry, Skip, and Cancel target the displayed provider. After Cancel, start a new run and confirm the previous run settles before any new send. This focused live check remains **Pending** until its result is recorded.
6. Generate an image on a supporting provider and confirm the workflow reaches completion without relying on text-only output.
7. Export Markdown and inspect provenance; create a new app session and verify history isolation.
8. In the installed build, open Settings, check for updates, switch themes and interface languages, and review the author/sponsor links. In the portable build, confirm update controls are hidden and `README-PORTABLE.txt` links to the latest GitHub Release. With Response language set to Auto, verify an English question receives English text and a Traditional Chinese question receives Traditional Chinese text regardless of the interface language; then verify a fixed response-language choice. In particular, confirm Grok answers the request instead of reproducing the internal `<response-language-policy>` block.
9. Export a sanitized debug bundle only if a failure occurs; never attach secrets or raw provider-page content.

## 回報方式

若你有 macOS 或 Linux 實機，最有價值的回報是：OS/CPU、app 版本、安裝方式、是否能第一次開啟、四家 provider 的登入／自動送出／完成偵測，以及不含私人內容的 debug bundle。Adapter 問題請使用 GitHub 的 **Adapter broken** 表單；安全問題請依 [`SECURITY.md`](../SECURITY.md) 私下回報。
