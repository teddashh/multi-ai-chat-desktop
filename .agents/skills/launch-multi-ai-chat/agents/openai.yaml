interface:
  display_name: "Launch Multi-AI Chat"
  short_description: "Open the local Tauri source app"
  default_prompt: "Launch the Multi-AI Chat Tauri app from this repository without an installer."
policy:
  allow_implicit_invocation: false
